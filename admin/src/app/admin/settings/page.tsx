// src/app/admin/settings/page.tsx — Phase 1c routing (placeholder)
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="设置"
      phase="后续阶段"
      description="账户 / 偏好设置 —— 修改密码、个人资料显示名、主题偏好。后端接口待定。"
    />
  );
}
