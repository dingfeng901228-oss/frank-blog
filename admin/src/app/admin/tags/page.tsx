// src/app/admin/tags/page.tsx — Phase 5 placeholder (Phase 1c routing)
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function TagsPage() {
  return (
    <PlaceholderPage
      title="Tags"
      phase="Phase 5 — Organization"
      description="Tags management — search, create, rename, delete. Shows usage count (how many posts use each tag). CRUD interface."
    />
  );
}
