// src/app/admin/categories/page.tsx — Phase 5 placeholder (Phase 1c routing)
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function CategoriesPage() {
  return (
    <PlaceholderPage
      title="Categories"
      phase="Phase 5 — Organization"
      description="Categories management — Blog categories vs Note categories, collection-aware. CRUD with rename, delete (with reference check), usage count per category."
    />
  );
}
