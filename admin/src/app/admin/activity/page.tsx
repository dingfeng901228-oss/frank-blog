// src/app/admin/activity/page.tsx — Phase 6 placeholder (Phase 1c routing)
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity"
      phase="Phase 6 — Revision / Activity"
      description="Activity log UI — reads from admin_logs table. Shows user actions (login, published, updated, uploaded, etc.) with timestamps. Filterable by user/action/date range."
    />
  );
}
