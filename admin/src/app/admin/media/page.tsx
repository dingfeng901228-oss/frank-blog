// src/app/admin/media/page.tsx — Phase 4 placeholder (Phase 1c routing)
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function MediaPage() {
  return (
    <PlaceholderPage
      title="Media Library"
      phase="Phase 4 — Media"
      description="Full media library: grid view, search by filename/alt, sort, upload (click/drag/paste), media details (preview, alt text, URL, copy, delete with reference check). R2 storage + D1 media metadata table."
    />
  );
}
