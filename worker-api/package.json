{
  "name": "frank-blog-cms-api",
  "version": "1.0.0",
  "private": true,
  "description": "frank-blog CMS API Worker — /api/admin/* endpoints (replaces Pages Functions)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
